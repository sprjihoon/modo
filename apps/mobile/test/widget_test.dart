// 모두의수선 기본 위젯 테스트
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // 기본 스모크 테스트
    // 실제 앱은 Supabase 초기화 등이 필요하여 별도 설정 필요
    expect(true, isTrue);
  });
}
