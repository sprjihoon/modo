# 우체국 API 테스트 결과 분석

## 테스트 결과

### ✅ API 호출 성공
- **운송장번호**: `6896770092992`
- **소포 예약번호**: `2025120156447005`
- **예약 일시**: `20251201214131` (2025-12-01 21:41:31)
- **접수 우체국**: `동대구우체국`
- **예상 접수요금**: `2150원`

### ⚠️ 확인 필요 사항

1. **개발 체크 로그 미출력**
   - `🔍 개발 체크 - testYn 파라미터` 로그가 보이지 않음
   - `🔍 개발 체크 - API 호출 전 파라미터 검증` 로그가 보이지 않음
   - `🔍 개발 체크 - URL 파라미터` 로그가 보이지 않음
   - `🔍 개발 체크 - regData 파라미터` 로그가 보이지 않음
   - `✅ 수거예약 상태 확인 결과` 로그가 보이지 않음
   
   **원인**: 코드가 아직 배포되지 않았을 가능성이 높습니다.

2. **수거예약 등록 여부 확인 필요**
   - API 호출은 성공했지만, 실제 수거예약이 등록되었는지는 확인 필요
   - `getResInfo` API를 호출하여 `treatStusCd` 확인 필요

## 다음 단계

### 1. 코드 배포
```bash
cd /Users/jangjihoon/modo
supabase functions deploy shipments-book
```

### 2. 수거예약 상태 확인
배포 후 다시 테스트하거나, 수동으로 확인:

**방법 A: Edge Function 배포 후 재테스트**
- Flutter 앱에서 다시 "🚚 실제 우체국 API" 버튼 클릭
- 개발 체크 로그 확인

**방법 B: 수동 확인 (현재 데이터 사용)**
```bash
# check_pickup_status.ts 스크립트 사용
deno run --allow-net --allow-env check_pickup_status.ts
```

### 3. 확인할 항목

#### testYn 파라미터 확인
- `testYn`이 `'N'`으로 설정되었는지 확인
- URL에 `testYn` 파라미터가 포함되지 않았는지 확인 (정상)

#### 수거예약 상태 확인
- `getResInfo` API 호출 결과에서 `treatStusCd` 확인:
  - `00`: 신청준비
  - `01`: 소포신청 (실제 수거예약 등록됨) ✅
  - `02`: 운송장출력
  - `03`: 집하완료

## 예상 결과

### 정상 동작 시
- ✅ `testYn`이 `'N'`으로 설정됨
- ✅ URL에 `testYn` 파라미터가 포함되지 않음
- ✅ `regData`에서 `testYn`이 제거됨
- ✅ `getResInfo` API 호출 성공
- ✅ `treatStusCd`가 `00` 또는 `01` (실제 수거예약 등록됨)

### 문제 발생 시
- ❌ `testYn`이 `'Y'`로 설정됨 → `test_mode` 파라미터 확인
- ❌ `getResInfo` API 호출 실패 → 파라미터 확인
- ❌ `treatStusCd`가 `00` 또는 `01`이 아님 → 우체국 고객센터 문의

## 참고 사항

- 현재 API 호출은 성공했지만, 실제 수거예약 등록 여부는 확인 필요
- 코드 배포 후 개발 체크 로그를 통해 상세 확인 가능
- 우체국 고객센터(1588-1300)에 송장번호로 문의하여 실제 수거예약 등록 여부 확인 가능

