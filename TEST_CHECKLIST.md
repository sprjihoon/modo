# 수거신청 테스트 체크리스트

## 테스트 전 확인 사항

### 1. 전화번호 설정 확인
- [ ] `📞 센터 전화번호 설정` 로그 확인
- [ ] `recTel`이 `01027239490`으로 설정되었는지 확인
- [ ] `source`가 "DB (ops_center_settings)" 또는 "환경변수" 또는 "기본값"인지 확인

### 2. testYn 파라미터 확인
- [ ] `🔍 개발 체크 - testYn 파라미터`: `testYn: "N"` 확인
- [ ] `🔍 개발 체크 - URL 파라미터`: `hasTestYn: false` 확인
- [ ] `🔍 개발 체크 - regData 파라미터`: `testYnRemoved: true` 확인

### 3. 예약일시 검증 확인
- [ ] `📅 날짜 정보`: 오늘, 내일 날짜 및 요일 확인
- [ ] `🔍 개발 체크 - API 응답 검증`: 예약일시 검증 결과 확인
- [ ] `⚠️ 예약일시가 이상합니다` 또는 `✅ 예약일시가 정상입니다` 로그 확인

### 4. 수거예약 상태 확인 (가장 중요!)
- [ ] `🔍 수거예약 상태 확인 API 호출` 로그 확인
- [ ] `✅ 수거예약 상태 확인 결과` 로그 확인:
  - `treatStusCd` 값 확인:
    - `00`: 신청준비
    - `01`: 소포신청 (실제 수거예약 등록됨) ✅
    - `02`: 운송장출력
    - `03`: 집하완료
- [ ] `✅ 수거예약이 정상적으로 등록되었습니다` 또는 `⚠️ 수거예약 상태가 예상과 다릅니다` 로그 확인

## 예상 로그 순서

1. `📞 센터 전화번호 설정` - 전화번호 설정 확인
2. `🔍 개발 체크 - testYn 파라미터` - testYn 값 확인
3. `🔍 개발 체크 - API 호출 전 파라미터 검증` - 전달되는 파라미터 확인
4. `✅ 실제 API 응답` - API 응답 데이터
5. `🔍 개발 체크 - API 응답 검증` - 예약일시 검증 결과
6. `🔍 수거예약 상태 확인 API 호출` - 수거예약 상태 확인 시작
7. `✅ 수거예약 상태 확인 결과` - 수거예약 등록 여부 확인 (가장 중요!)

## 문제 발생 시 확인 사항

### 전화번호가 여전히 `01000000000`인 경우
- `ops_center_settings` 테이블에 전화번호가 설정되어 있는지 확인
- 환경 변수 `CENTER_PHONE`이 설정되어 있는지 확인
- 로그에서 `source` 값 확인

### 예약일시가 이상한 경우
- 우체국 API 서버 시간 설정 확인 필요
- 우체국 고객센터(1588-1300)에 문의

### 수거예약 상태 확인 API 호출 실패
- `❌ 수거예약 상태 확인 API 호출 실패` 로그 확인
- 에러 메시지 확인
- 파라미터 확인 (`reqYmd` 값이 올바른지)

## 성공 기준

✅ **테스트 성공 조건:**
1. `testYn`이 `'N'`으로 설정됨
2. 전화번호가 `01027239490`으로 설정됨
3. API 호출 성공 (운송장번호 발급)
4. `getResInfo` API 호출 성공
5. `treatStusCd`가 `00` 또는 `01` (실제 수거예약 등록됨)

## 참고 사항

- 예약일시(`resDate`)는 우체국 API 응답에서 받는 값입니다
- 예약일시와 실제 픽업일이 다를 수 있습니다
- 실제 수거예약 등록 여부는 `treatStusCd` 값으로 확인할 수 있습니다

